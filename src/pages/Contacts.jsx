import Map from '../ui/Map';

function Contacts() {
    return (
        <div>
            <div>Contacts</div>
            <Map mapSize={{ width: '600px', height: '400px' }} />
        </div>
    );
}

export default Contacts;
